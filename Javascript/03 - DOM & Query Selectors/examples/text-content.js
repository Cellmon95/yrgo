const element = document.querySelector('p');

// With textContent we can change the content between the <p> tags.
element.textContent = 'Great Scott!';

// If you just want to fetch the current textContent
console.log(element.textContent);
